pub struct Tileset {
    pub texture: Rc<Texture>,
    pub texture_width: u32,
    pub texture_height: u32,
    pub tile_width: u32,
    pub tile_height: u32,
    pub tile_count: u32,
    pub margin: u32,
    pub spacing: u32,
}

impl Tileset {
    
}
